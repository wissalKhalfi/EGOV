{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CarteEdinar creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('carteedinar') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
