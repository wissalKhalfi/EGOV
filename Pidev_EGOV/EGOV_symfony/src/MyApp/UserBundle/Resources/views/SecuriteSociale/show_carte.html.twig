{#{% extends "EGOVVisitorBundle:Visitor:affiche_carte.html.twig" %}#}

{% block content %}
    {% block body -%}
        <div class="container  text-center">
            <h1>Carte  Sécurité Sociale</h1>
        </div>


        <section class="section">
            <div class="container ">
                <table class="table">
                    <thead>
                    </thead>
                    <tbody>


                        <tr>
                        <th>la date de délivrance :</th>
                        <td>{{ securitesociale.proprietaire}}</td>
                        </tr>
                        <tr>
                        <th>id epoux</th>
                        <td>{{ securitesociale.conjoint}}</td>
                        </tr>
                        <tr>
                        <th>id epouse</th>
                        <td>{{ securitesociale.nbreEnfants }}</td>
                        </tr>
                        <tr>
                        <th>validation</th>
                        <td>{{ securitesociale.dateDelivrenceCsc |date('Y-m-d') }}</td>
                        </tr>

                        <tr>
                        <th>validation</th>
                        <td>{{ securitesociale.dateValiditeCsc |date('Y-m-d') }}</td>
                        </tr>

                       
                        {#
                        <tr>
                        <td><a class="btn btn-sm btn-default edit_link" href="{{ path('edit_passeport_user', { 'numPasseport' : p.numPasseport }) }}"><i
                                    class="fa fa-download"></i>edit</a>

                       </tr>

                                                #}
                            {#  <tr>
                              <th>nom</th>
                              <td>{% if p.utilisateur.nom is defined %}
                                  {{ p.utilisateur.nom }}
                                  {% endif %}</td>
                                  </tr>
      
                                  <tr>
                                  <th>prenom</th>
                                  <td>{{ user.prenom }}</td>
                                  </tr>
      
                                  <tr>
                                  <th>date de naissance</th>
                                  <td>{{ user.dateNaissance|date('Y-m-d') }}</td>
                                  </tr>
      
                                  <tr>
                                  <th>lieu de naissance</th>
                                  <td>{{ user.lieuNaissance }}</td>
                                  </tr>
      
                                  <tr>
                                  <th>adresse</th>
                                  <td>{{ user.adresse }}</td>
                                  </tr>
      
                                  <tr>
                                  <th>num cin </th>
                                  <td>{{ user.numCin }}</td>
                                  </tr> #}
                    </tbody>
                </table>


            {% endblock %}
        {% endblock %}
